# M-Control v1.3.31

发布日期：2026-07-25

本版本正式加入自研 MSI WMI/ACPI Provider，并修复 Mystic Light 24 区逐键预览和响应式仪表图标布局问题。

## 主要更新

- 新增 clean-room 自研 MSI WMI/ACPI MOF Provider，不打包 `msiapcfg.dll`、MSIService、Service2、MSIWMIACPI2.dll 或 NBFoundation。
- 安装版默认注册自研 Provider；便携版和普通启动也会自动确保 Provider 可用。已有有效外部 Provider 不覆盖，检测到第三方改写时不抢回。
- 注册后优先通过系统 `mofcomp.exe` 热加载，成功时无需重启；卸载时仅在注册值仍由 M-Control 所有的情况下恢复旧值。
- 修复 24 区 Mystic Light 逐键编辑器第一排显示整串 `zone-1,zone-2...` 而不是区域编号的问题；不改变 HID 报文和区域顺序。
- 修复窗口缩小后 CPU/GPU 仪表小图标向左偏移的问题，使图标继续与固定圆弧、数字和单位共用中心线。
- 继续保留 Mystic Light MCU 官方 0-4 档固件亮度显示与写后确认。

## 发布包

- `MControl_v1.3.31_Setup.exe`：普通安装版，可直接覆盖升级。
- `MControl_v1.3.31_Portable.zip`：普通便携版，完整解压后运行。
- `MControl_v1.3.31_Setup_WebView2_Fixed.exe`：内嵌微软官方 x64 Fixed Version Runtime 的安装版，适合系统 WebView2 缺失或异常的环境。
- `SHA256SUMS.txt`：正式产物 SHA256 校验清单。

`v1.3.31` 属于 hotfix；hotfix 可以提供 Fixed Runtime 安装版，但不提供 Fixed Runtime 便携 ZIP。

## 验证结果

- 重启后实机验证：Provider 日志为 `provider-activated`，`restartRequired=False`；EC、温度、风扇、功耗和电池通道读取正常。
- 5 个正式前端脚本语法检查全部通过。
- 133 项 Node 静态与行为测试全部通过。
- 301 项 .NET 测试全部通过。
- Release 构建 0 警告、0 错误。
- Portable ZIP 通过 7-Zip 完整性测试，MOF resource-only DLL 构建哈希稳定为 `11E929E92735F006034E9F097DDD8FE51C69F2BB70ACBBF36B53A4116A19D779`。

## SHA256

```text
44059C8176B6F8D63A2BEB993915BB3AC2D108A4414AAD74CEE546D634D0D192  MControl_v1.3.31_Portable.zip
553FCC9423235AEC037B05E2FB0AA5458442665ECEAB78AD045E390FAF4B7074  MControl_v1.3.31_Setup.exe
1349E8DE98E16C7F43A0BF38EEE3A6FCA36910E7CF96E669A698B532A5C9E41D  MControl_v1.3.31_Setup_WebView2_Fixed.exe
```

## 升级提示

- `v1.1.0` 或更高版本可直接运行安装包覆盖升级。
- 便携版不会自动覆盖自身文件；检查到新版本后会打开对应 Release 页面。
- 自研 Provider 仍需按机型、BIOS/EC 和现有 MSI WMI 状态验证；卸载恢复和其他机型回归请保留诊断包。
